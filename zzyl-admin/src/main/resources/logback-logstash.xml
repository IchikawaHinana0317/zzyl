<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <property name="log.path" value="${LOG_PATH:-/app/logs}" />

    <property
            name="log.pattern"
            value="%d{HH:mm:ss.SSS} [%thread] %-5level %logger{20} - [%method,%line] - %msg%n"
    />

    <appender
            name="console"
            class="ch.qos.logback.core.ConsoleAppender"
    >
        <encoder>
            <pattern>${log.pattern}</pattern>
        </encoder>
    </appender>

    <appender
            name="logstash"
            class="net.logstash.logback.appender.LogstashTcpSocketAppender"
    >
        <destination>
            ${LOGSTASH_DESTINATION:-host.docker.internal:5044}
        </destination>

        <encoder
                charset="UTF-8"
                class="net.logstash.logback.encoder.LogstashEncoder"
        >
            <customFields>
                {"app":"zzyl-admin"}
            </customFields>
        </encoder>
    </appender>

    <!-- 这里继续保留原有 file_info、file_error、sys-user -->

    <logger name="com.zzyl" level="info" />
    <logger name="org.springframework" level="warn" />

    <logger
            name="sys-user"
            level="info"
            additivity="false"
    >
        <appender-ref ref="sys-user" />
    </logger>

    <root level="info">
        <appender-ref ref="console" />
        <appender-ref ref="logstash" />
        <appender-ref ref="file_info" />
        <appender-ref ref="file_error" />
    </root>

</configuration>